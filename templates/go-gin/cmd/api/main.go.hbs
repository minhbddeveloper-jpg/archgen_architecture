package main

import (
	"net/http"

	"github.com/gin-gonic/gin"
{{#if hasEntities}}
	"{{moduleName}}/internal/presentation/handlers"
{{/if}}
)

func main() {
	router := gin.Default()

	router.GET("/health", func(context *gin.Context) {
		context.JSON(http.StatusOK, gin.H{
			"status":  "ok",
			"service": "{{projectSlug}}",
		})
	})
{{#each entities}}
	handlers.Register{{className}}Routes(router)
{{/each}}

	if err := router.Run(); err != nil {
		panic(err)
	}
}
