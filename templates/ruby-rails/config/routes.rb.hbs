Rails.application.routes.draw do
  get "/health", to: proc {
    [200, { "Content-Type" => "application/json" }, ['{"status":"ok","service":"{{projectSlug}}"}']]
  }
{{#each entities}}
  resources :{{routeName}}
{{/each}}
end
