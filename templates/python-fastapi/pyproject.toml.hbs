[project]
name = "{{projectSlug}}"
version = "0.1.0"
requires-python = ">={{languageVersion}}"
dependencies = [
  "fastapi>={{frameworkVersion}}",
  "uvicorn[standard]>={{uvicornVersion}}"{{#if orm}},
  "sqlalchemy>=2.0.0",
  "psycopg[binary]>=3.1.0"{{/if}}
]

[tool.uvicorn]
factory = false
