from django.http import JsonResponse
from django.urls import path
{{#each entities}}
from presentation.views.{{moduleName}}_views import {{moduleName}}_collection, {{moduleName}}_member
{{/each}}


def health(_request):
    return JsonResponse({"status": "ok", "service": "{{projectSlug}}"})


urlpatterns = [
    path("health", health),
{{#each entities}}
    path("{{routeName}}", {{moduleName}}_collection),
    path("{{routeName}}/", {{moduleName}}_collection),
    path("{{routeName}}/<str:record_id>", {{moduleName}}_member),
{{/each}}
]
