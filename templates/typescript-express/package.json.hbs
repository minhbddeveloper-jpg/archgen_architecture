{
  "name": "{{projectSlug}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/main.ts",
    "build": "tsc",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "express": "^{{frameworkVersion}}"{{#if orm}},
    "@prisma/client": "^5.20.0"{{/if}}
  },
  "devDependencies": {
    "@types/express": "^{{expressTypesVersion}}",
    "@types/node": "^{{nodeTypesVersion}}",
    "tsx": "^{{tsxVersion}}",
    "typescript": "^{{languageVersion}}"{{#if orm}},
    "prisma": "^5.20.0"{{/if}}
  }
}
