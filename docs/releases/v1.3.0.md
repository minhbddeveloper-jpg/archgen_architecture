# arxgen v1.3.0

## Added

- Added `arxgen upgrade schema --from-sql <file>` for generated TypeScript Express projects.
- Expanded `arxgen upgrade schema --from-sql <file>` to patch generated backend entity/model files for:
  - TypeScript NestJS
  - Python FastAPI
  - Python Django
  - Java Spring Boot
  - C# ASP.NET Core
  - PHP Laravel
  - Go Gin
  - Ruby Rails
  - Kotlin Ktor
- Added cross-stack CRUD creation for new SQL tables during schema upgrade.
- Added route/module registration updates for newly created backend entities where required by the generated stack.
- Added schema diff support for additive SQL changes.
- Added dry-run preview output for schema upgrades.
- Added safe patching for:
  - domain entity fields
  - validation schemas
  - create use cases
  - Prisma models
- Added automatic creation of new entities found in the upgraded SQL schema.

## Safety Rules

- Schema upgrade does not delete fields.
- Schema upgrade does not rename fields automatically.
- Destructive database changes must still be handled manually.

## Validation

- Added tests for dry-run preview and real schema upgrade behavior.
- Added cross-stack schema upgrade tests for generated backend entity/model files.
- Added cross-stack schema upgrade tests for new table CRUD creation and route/module registration.
- Verified with `npm test`.
